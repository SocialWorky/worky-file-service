apiVersion: v1
kind: Service
metadata:
  name: file-service-svc
  namespace: social-network-dev
spec:
  selector:
    app: file-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3005
  type: ClusterIP
